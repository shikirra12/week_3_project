let keys = document.getElementsByTagName('span');
let display = document.querySelector('.display'
)
let value;

for (var i = 0; i < keys.length; i++) {
  keys[i].onclick = function (action) {
    
  }
  return answer;
}
